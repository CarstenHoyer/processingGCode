class Creator {
  ArrayList<Path> paths = new ArrayList<Path>();
  Device printer;
  Settings settings;
  Creator(Device t_printer, Settings t_settings) {
    printer = t_printer;
    settings = t_settings;
  }
}
